#include <stdio.h>

typedef unsigned char byte; // Garante que 'byte' tenha sempre 1 byte
typedef unsigned long long uint64; // Garante 64 bits sem sinal

int main() {
	byte = PosiSmall;
	uint64 = PosiBig;
	
    return 0;
}