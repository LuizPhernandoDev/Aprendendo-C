#include <stdio.h>

int main() {
	int matriz[3][3] = {{1, 2, 3},  // Primeira linha (índice 0)
    					{4, 5, 6};  // Segunda linha (índice 1)
						{7, 8, 9}   // terceira linha (índice 2)
    							 };
								 
    return 0;
}